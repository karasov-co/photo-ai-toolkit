## What this changes

<!-- One or two sentences. Link the issue if there is one: Closes #123 -->

## Why

## How it was verified

- [ ] `pytest` passes locally
- [ ] `ruff check .` is clean
- [ ] Added or updated tests for the change
- [ ] Ran `python main.py --dry-run` against a sample folder, if the pipeline changed

## Notes for the reviewer

<!-- If this changes the scoring prompt or the CSV/JSON schema, say so explicitly:
     both are load-bearing for anyone re-running the tool over an existing results file. -->
