openai==2.53.0
# Explicit, not transitive. Every deterministic metric in this project is
# NumPy -- Laplacian variance, structure tensors, phase correlation -- and it
# was arriving only because rawpy happens to depend on it.
numpy==2.5.1
Pillow==12.3.0
rawpy==0.27.0
exifread==3.5.1
imagehash==4.3.2
python-dotenv==1.2.2
tqdm==4.70.0
pillow-heif==1.5.0

# Not here on purpose: onnxruntime. Semantic similarity is an optional extra --
#   pip install -e ".[embeddings]"   then   photoai models --download
# The tool runs with none of it and says which similarity it used either way.
